import { FooterContent } from '@/features/footer';

export const Footer = () => {
  return (
    <footer className={'h-[30vh] xl:h-[70vh] relative bg-bg-primary overflow-hidden'}>
      <FooterContent />
    </footer>
  );
};
